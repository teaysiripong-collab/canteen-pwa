# AGENTS.md

เอกสารนี้เป็นกฎกลางสำหรับ AI ทุกตัวที่เข้ามาทำงานในโปรเจกต์ `canteen-pwa` เช่น Codex, ChatGPT หรือเครื่องมือช่วยเขียนโค้ดอื่น ๆ

## 1. ลำดับการอ่านก่อนเริ่มงาน

ก่อนแก้ไขโค้ดทุกครั้ง ให้ทำตามลำดับนี้

1. อ่าน `AGENTS.md`
2. อ่าน `PROJECT_CONTEXT.md`
3. อ่าน `TASKS.md`
4. อ่าน `CHANGELOG.md`
5. ตรวจสอบไฟล์ที่เกี่ยวข้องกับงานจริงก่อนแก้ไข

ห้ามเริ่มเขียนโค้ดจากการเดาโครงสร้างระบบ

## 2. เป้าหมายของระบบ

พัฒนาเว็บแอปจัดการงานแคนทีน MEKTEC ที่ใช้งานง่ายบนมือถือ รองรับงานสต๊อก วัตถุดิบ สูตรอาหาร เมนู การสั่งซื้อ การโอนย้ายระหว่างตึก และรายงานสำหรับหัวหน้างาน

## 3. ภาษาที่ใช้

- อธิบายผลการทำงานและคำแนะนำแก่ผู้ใช้เป็นภาษาไทย
- ข้อความบนหน้าจอสำหรับพนักงานใช้ภาษาไทยเป็นหลัก
- ชื่อตัวแปร ฟังก์ชัน คลาส และชื่อไฟล์ใช้ภาษาอังกฤษ
- เขียนชื่อให้สื่อความหมาย หลีกเลี่ยงตัวย่อที่อ่านไม่รู้เรื่อง

## 4. กฎการแก้ไขโค้ด

- รักษาฟังก์ชันเดิมที่ยังใช้งานได้
- ห้ามลบฟีเจอร์เดิมโดยไม่ได้รับคำสั่งชัดเจน
- แก้เฉพาะส่วนที่เกี่ยวข้องกับงาน ลดผลกระทบต่อระบบส่วนอื่น
- ห้ามใส่ข้อมูลลับ รหัสผ่าน Token หรือ API Key ลงใน Repository
- ตรวจสอบค่าที่ผู้ใช้กรอกก่อนบันทึกทุกครั้ง
- แสดงข้อความผิดพลาดเป็นภาษาไทยที่ผู้ใช้เข้าใจได้
- งานที่มีข้อมูลจำนวนมากต้องไม่ทำให้หน้าเว็บค้างโดยไม่จำเป็น
- ฟีเจอร์ใหม่ต้องรองรับหน้าจอมือถือก่อน แล้วจึงปรับสำหรับเดสก์ท็อป
- งานสต๊อกต้องรักษาความถูกต้องของจำนวน หน่วย สถานที่ และประวัติรายการ
- วันที่และเวลาใช้เขตเวลา `Asia/Bangkok`

## 5. ขั้นตอนการทำงานต่อหนึ่งงาน

1. สรุปความเข้าใจของงาน
2. ระบุไฟล์ที่คาดว่าจะได้รับผลกระทบ
3. ตรวจสอบโค้ดเดิมและข้อมูลเดิม
4. ลงมือแก้ไขด้วยขอบเขตที่เล็กที่สุด
5. ทดสอบกรณีใช้งานปกติและกรณีผิดพลาด
6. ตรวจสอบการใช้งานบนมือถือ
7. อัปเดต `TASKS.md`
8. อัปเดต `CHANGELOG.md`
9. สรุปสิ่งที่แก้ ความเสี่ยง และวิธีทดสอบ

## 6. Definition of Done

งานถือว่าเสร็จเมื่อ

- ฟีเจอร์ทำงานตามความต้องการ
- ไม่มี error ที่มองเห็นได้ใน Console จากส่วนที่แก้
- ไม่ทำให้ฟีเจอร์เดิมเสีย
- มี validation และข้อความแจ้งเตือนที่เหมาะสม
- ใช้งานบนมือถือได้
- เอกสารงานถูกอัปเดต
- ระบุวิธีทดสอบให้ผู้ใช้ตรวจซ้ำได้

## 7. กฎด้าน Git และ Pull Request

- ห้ามทำงานฟีเจอร์โดยตรงบน Branch หลัก
- หนึ่งงานหลักควรอยู่ในหนึ่ง Branch
- ชื่อ Branch ใช้รูปแบบ เช่น `feature/expiry-alerts`, `fix/save-button`, `docs/project-context`
- Commit ควรเล็กและระบุสิ่งที่เปลี่ยนชัดเจน
- เปิด Pull Request แบบ Draft เมื่อยังทดสอบไม่ครบ
- Pull Request ต้องมีสรุป การทดสอบ ความเสี่ยง และภาพหน้าจอเมื่อมีการแก้ UI
- ห้าม Merge หากยังพบข้อผิดพลาดสำคัญหรือข้อมูลอาจเสียหาย

## 8. หลักการข้อมูลสต๊อก

- ห้ามปรับยอดคงเหลือโดยไม่มีประวัติรายการ
- รายการรับเข้า เบิก โอน และปรับยอดต้องระบุผู้ทำรายการและเวลา
- การเบิกวัตถุดิบที่มี Lot หรือวันหมดอายุ ให้เตรียมรองรับ FEFO
- จำนวนต้องไม่ติดลบ เว้นแต่มีข้อกำหนดและการอนุมัติชัดเจน
- หน่วยของวัตถุดิบต้องสอดคล้องกันก่อนคำนวณ
- การลบข้อมูลควรใช้สถานะยกเลิกหรือ Soft Delete เมื่อประวัติมีผลต่อรายงาน

## 9. หลักการ UI/UX

- ใช้งานง่ายสำหรับพนักงานที่ไม่ได้เชี่ยวชาญคอมพิวเตอร์
- ปุ่มหลักต้องเห็นชัดและกดง่ายบนมือถือ
- ฟอร์มจำนวนมากควรแบ่งเป็นขั้นตอนหรือกลุ่ม
- ใช้สีแดงสำหรับปัญหา/อันตราย สีเหลืองสำหรับเตือน และสีเขียวสำหรับสำเร็จ
- ห้ามใช้สีเป็นวิธีสื่อความหมายเพียงอย่างเดียว ต้องมีข้อความหรือไอคอนด้วย
- ตารางต้องมี Search, Filter และสถานะโหลดข้อมูลเมื่อเหมาะสม

## 10. เมื่อข้อมูลไม่ชัดเจน

หากรายละเอียดบางส่วนยังไม่ชัด แต่สามารถทำงานแบบปลอดภัยได้ ให้เลือกค่าเริ่มต้นที่ย้อนกลับได้และบันทึกสมมติฐานไว้ใน Pull Request ห้ามสร้างข้อมูลธุรกิจสำคัญขึ้นเอง เช่น ราคา ปริมาณสูตร สิทธิ์ผู้ใช้ หรือยอดสต๊อกจริง
